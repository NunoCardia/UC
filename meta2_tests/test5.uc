int main(void){
    a = a + b;
    b = 1 + 3;
    c = a + b + c + d;
    d = 1 + a + c;
    e = a * 2;
    f = a * b *c;
    g = a * b / 2;
    h = a - a - b * c;
    i = p % 20 + c % 30;
    j = 1 * h - 6 + p;
    l = p - o;
    m = (a + b) + (z - p);
    n = a;
    p = bla(b, c, z);
    q = b < c % 20 * c - a + q - aass(b == c + p);
    a,b,c = 0;
}
